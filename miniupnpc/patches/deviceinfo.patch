diff -r 380d72f0911d miniupnpc/igd_desc_parse.c
--- a/miniupnpc/igd_desc_parse.c	Sat Jul 12 10:46:06 2025 +0200
+++ b/miniupnpc/igd_desc_parse.c	Sat Jul 12 10:46:38 2025 +0200
@@ -81,6 +81,15 @@
 		dstmember = datas->tmp.scpdurl;
 /*	else if( !strcmp(datas->cureltname, "deviceType") )
 		dstmember = datas->devicetype_tmp;*/
+	else if( !strcmp(datas->cureltname, "friendlyName") )
+		dstmember = datas->tmp.friendlyName;
+	else if( !strcmp(datas->cureltname, "manufacturer") )
+		dstmember = datas->tmp.manufacturer;
+	else if( !strcmp(datas->cureltname, "modelName") )
+		dstmember = datas->tmp.modelName;
+	else if( !strcmp(datas->cureltname, "modelNumber") )
+		dstmember = datas->tmp.modelNumber;
+
 	if(dstmember)
 	{
 		if(l>=MINIUPNPC_URL_MAXSIZE)
diff -r 380d72f0911d miniupnpc/igd_desc_parse.h
--- a/miniupnpc/igd_desc_parse.h	Sat Jul 12 10:46:06 2025 +0200
+++ b/miniupnpc/igd_desc_parse.h	Sat Jul 12 10:46:38 2025 +0200
@@ -29,6 +29,10 @@
 	/*! \brief serviceType */
 	char servicetype[MINIUPNPC_URL_MAXSIZE];
 	/*char devicetype[MINIUPNPC_URL_MAXSIZE];*/
+	char friendlyName[MINIUPNPC_URL_MAXSIZE];
+	char manufacturer[MINIUPNPC_URL_MAXSIZE];
+	char modelName[MINIUPNPC_URL_MAXSIZE];
+	char modelNumber[MINIUPNPC_URL_MAXSIZE];
 };
 
 /*! \brief Structure to store the result of the parsing of UPnP
